# ------------------------------------
# Student Progress Management System
# Environment Variables Template
# ------------------------------------

# --- Server Configuration ---
# The port the backend server will run on
PORT=8000
# The origin URL of your frontend application for CORS policy
CORS_ORIGIN=http://localhost:5173

# --- Database Configuration ---
# Your MongoDB connection string (e.g., from MongoDB Atlas)
MONGO_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net
# The name of the database to be used within your MongoDB cluster
DB_NAME=student-progress

# --- Nodemailer (Gmail) Configuration for Sending Emails ---
# Host for the email service provider (for Gmail, this is standard)
MAIL_HOST=smtp.gmail.com
# Port for the email service (587 is standard for TLS)
MAIL_PORT=587
# Your full Gmail address (e.g., your.email@gmail.com)
MAIL_USER=
# The 16-character App Password generated from your Google Account settings
MAIL_PASS=
